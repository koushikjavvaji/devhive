# Optional: enables the "gpt" teammate. Without this, the room still works
# with triage-bot and (if a checkpoint exists) from-scratch-gpt.
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini

# Optional overrides — defaults shown
# DEVHIVE_DB_PATH=data/devhive.db
# DEVHIVE_CHECKPOINT_PATH=checkpoints/ckpt.pt
# DEVHIVE_TOKENIZER_PATH=tokenizer/vocab.json
